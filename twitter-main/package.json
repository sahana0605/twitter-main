{
  "name": "twitter-main-root",
  "private": true,
  "scripts": {
    "frontend": "npm --prefix ./frontend/twitterclone start",
    "backend": "npm --prefix ./backend run dev",
    "start": "npm-run-all --parallel frontend backend",
    "dev": "npm-run-all --parallel frontend backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "npm-run-all": "^4.1.5"
  }
}
